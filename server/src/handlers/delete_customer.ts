export async function deleteCustomer(id: number): Promise<void> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is soft-deleting a customer by setting is_active to false.
    // Should validate that the customer exists and handle related invoices properly.
    return Promise.resolve();
}