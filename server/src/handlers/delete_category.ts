export async function deleteCategory(id: number): Promise<void> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is soft-deleting a category by setting is_active to false.
    // Should validate that the category exists and has no active products associated.
    return Promise.resolve();
}