export async function deleteTax(id: number): Promise<void> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is soft-deleting a tax by setting is_active to false.
    // Should validate that the tax exists and is not being used by active products.
    return Promise.resolve();
}